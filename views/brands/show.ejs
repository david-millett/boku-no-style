<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= brand.name %> - Boku no Style</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <main>
        <div class="hero">
            <div class="main-title">
                <h1><%= brand.name %></h1>
            </div>
            <img src="/<%= brand.logo %>" alt="<%= brand.name %>'s logo" class="hero-image"/>
        </div>
        <div class="stats">
            <p><a href="<%= brand.website %>" target="_blank">Website</a></p>
            <p>Established: <%= brand.established %></p>
            <p>Price range: <%= brand.priceRange %></p>
        </div>
        <div class="favourites">
            <h3>Add to favourites</h3>
            <p><%= brand.fans.length %></p>
            <% if (brand.fans.some(fan => fan.equals(user._id))) { %>
                <!-- If user has already favourited, remove from fans array -->
                <form action="/brands/<%= brand._id %>/favourites?_method=DELETE" method="POST">
                    <button>Remove from favourites</button>
                </form>
            <% } else { %>
                <!-- If user is not a fan, add to fans array -->
                <form action="/brands/<%= brand._id %>/favourites" method="POST">
                    <button>Add to favourites</button>
                </form>
            <% } %>
        </div>
        <p><%= brand.description %></p>
        <a href="/brands/<%= brand._id %>/edit"><button>Edit <%= brand.name %></button></a>
        <form action="/brands/<%= brand._id %>?_method=DELETE" method="POST">
            <button type="submit">Delete this brand</button>
        </form>
        <p>This brand was added by <%= brand.addedBy.username %></p>
        <a href="/brands">Back to brands</a>
    </main>
</body>
</html>